apiVersion: v1
kind: Service
metadata:
  name: {{ include "application-with-subcharts.fullname" . }}
  labels:
    {{- include "application-with-subcharts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "application-with-subcharts.selectorLabels" . | nindent 4 }}
