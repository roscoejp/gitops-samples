apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "application-with-subcharts.fullname" . }}-test-connection"
  labels:
    {{- include "application-with-subcharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "application-with-subcharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
